<partial name="_PartialThe" />
<!DOCTYPE html>
<html>
<head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />

    <link rel="shortcut icon" href="/favicon.ico" type="image/x-icon" />

    <title>@ViewData["Title"] NET牛人</title>

    <partial name="_PartialMetaSEO" />

    <partial name="_PartialLayoutQuote4" />

    <partial name="_PartialJzQuote" />

    <link href="/css/global.css" rel="stylesheet" asp-append-version="true" />

</head>
<body>
    @{
        var vrv = ViewContext.RouteData.Values;
        string ca = (vrv["controller"].ToString() + "/" + vrv["action"].ToString()).ToLower();

        var canav = "account/login,account/register,home/auth,mix/log,mix/logchart,mix/terms".Split(',');
        var cafooter = "account/login,account/register,home/auth,home/write,store/nenos,store/qqcos,store/qnkodo,services/keyvalues,user/write,user/message,user/verify,user/setting,mix/log,mix/logchart,mix/terms,note/index".Split(',');
    }

    @if (!canav.Contains(ca))
    {
        <partial name="_PartialNav" />
    }

    <div class="min200">
        @RenderBody()
    </div>

    @if (!cafooter.Contains(ca))
    {
        <partial name="_PartialFooter" />
    }

    @if (!canav.Contains(ca))
    {
        <a href="javascript:void(0);" onclick="$('html,body').animate({scrollTop:0}, 400)" class="totop" title="返回顶部">ˆ</a>
    }

    <partial name="_PartialFun" />
</body>
</html>